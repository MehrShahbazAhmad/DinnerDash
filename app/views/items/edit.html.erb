<div class="container">
  <h1>Editing Item for '<%= @restaurant.title %>'</h1>
  <%= render 'form', restaurant: @restaurant %>
  <%= link_to 'Back', restaurant_path(@restaurant), class:"btn btn-dark"%>
</div>
